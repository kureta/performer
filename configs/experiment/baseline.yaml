# @package _global_

# to execute this experiment run:
# python train.py experiment=example

tags: ["alpha"]

seed: 12345

trainer:
  min_epochs: 10
  max_epochs: 10000

model:
  n_harmonics: 180
  n_filters: 195
  lr: 0.001

datamodule:
  batch_size: 8

logger:
  wandb:
    tags: ${tags}
    group: "gru"
