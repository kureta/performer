# @package _global_

# to execute this experiment run:
# python train.py experiment=example

tags: ["violin"]

seed: 12345

trainer:
  min_epochs: 10
  max_epochs: 10000

model:
  n_harmonics: 112
  n_filters: 128
  lr: 0.0003
  weight_decay: 0.1

datamodule:
  data_path: ${paths.data_dir}/violin_samples.pth
  wav_dir: /home/kureta/Music/Violin Samples
  batch_size: 8
  fmin: 190

logger:
  wandb:
    tags: ${tags}
